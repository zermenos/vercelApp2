import { Bytes, Node } from '../../types';
import { ITreeStorage } from '../../types/storage';
import { Hash } from '../hash/hash';
export declare class IndexedDBStorage implements ITreeStorage {
    #private;
    private readonly _prefix;
    static readonly storageName = "merkle-tree";
    private readonly _prefixHash;
    private readonly _store;
    constructor(_prefix: Bytes, databaseName?: string);
    get(k: Bytes): Promise<Node | undefined>;
    put(k: Bytes, n: Node): Promise<void>;
    getRoot(): Promise<Hash>;
    setRoot(r: Hash): Promise<void>;
}
